import { ConfirmEqualDirective } from './confirm-equal.directive';

describe('ConfirmEqualDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualDirective();
    expect(directive).toBeTruthy();
  });
});
